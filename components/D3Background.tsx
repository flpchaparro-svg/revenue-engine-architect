import React from 'react';

// DISABLED per user request. 
// The Hero Section now relies solely on HeroVisual.tsx (The Constellation).
const D3Background: React.FC = () => {
  return null;
};

export default D3Background;